package com.tracker.activity.domain;

/** Which {@link ActivityCandidate} field produced the winning score for a match (issue #66). */
public enum MatchField {
    NAME, DESCRIPTION, CATEGORY, NONE
}
